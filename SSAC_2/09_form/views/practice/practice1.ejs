<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습문제 1</title>
  </head>

  <body>
    <!-- 이름, 성별, 생년월일, 관심사 를 포함하여 4개 이상의 정보 입력받기
    이름의 input type은 text
    성별의 input type은 radio button
    생년월일의 input type은 select
    관심사의 input type은 checkbox -->

    <h2>get으로 정보받기</h2>
    <form action="/getPractice1" method="get">
      <div class="form-group">
        <label>이름: <input type="text" name="name" /> </label>
      </div>
      <br />

      <div class="form-group">
        성별<br />
        <label> <input type="radio" name="gender" value="남자" /> 남자 </label>
        <label> <input type="radio" name="gender" value="여자" /> 여자 </label>
      </div>
      <br />

      <div class="form-group">
        생년월일<br />
        <select name="year" id="yearSelect">
          <% for (let i = 2000; i <= 2010; i++) { %>
          <option value="<%=i%>"><%=i%></option>
          <% } %></select
        >년
        <select name="month" id="monthSelect">
          <% for(let i=1; i <=12;i++){ %>
          <option value="<%=i%>"><%=i%></option>
          <% } %></select
        >월
        <select name="day" id="daySelect">
          <% for(let i=1; i <=31;i++){ %>
          <option value="<%=i%>"><%=i%></option>
          <% } %></select
        >일
      </div>
      <br />

      <div class="form-group">
        관심사<br />
        <label>
          <input type="checkbox" name="favorite" value="여행" /> 여행
        </label>
        <br />
        <label>
          <input type="checkbox" name="favorite" value="패션" /> 패션
        </label>
        <br />
        <label>
          <input type="checkbox" name="favorite" value="음식" /> 음식
        </label>
      </div>

      <div class="form-group">
        <button type="submit">회원가입</button>
      </div>
    </form>
  </body>
</html>
